debug: true

server:
  port: 8443
  ssl:
    enabled: true
    key-store-type: PKCS12
    key-store: classpath:server-keystore.p12
    key-store-password: server-p12-pwd
    client-auth: none
spring:
  threads:
    virtual:
      enabled: true
web:
  client:
    one:
      name: one
      description: one
    two:
      name: two
      description: two
      retryCount: 5
